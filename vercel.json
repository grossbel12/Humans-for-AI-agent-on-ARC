{
  "framework": "nextjs",
  "buildCommand": "prisma migrate deploy && prisma generate && next build",
  "functions": {
    "app/api/**": {
      "maxDuration": 30
    },
    "app/api/v1/agents/**": {
      "maxDuration": 60
    }
  }
}
